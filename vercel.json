{
  "version": 2,
  "builds": [
    {
      "src": "yarn && yarn build && yarn start",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "handle": "vercel", "src": "/.*" }],
  "output": "public"
}
